name = "hp-chat"
main = "src/index.ts"
compatibility_date = "2026-07-01"

# Publish on <name>.<subdomain>.workers.dev so the site can reach it.
workers_dev = true

# The Groq key is NOT stored here. Set it as an encrypted secret:
#   npx wrangler secret put GROQ_API_KEY
